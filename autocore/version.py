from __future__ import annotations

AUTOCORE_VERSION = "0.1.0"
AUTOCORE_UI_MIN_VERSION = "0.1.0"

AUTOCORE_CAPABILITIES = {
    "build_auditor": True,
    "codex_companion": True,
    "guided_audit": True,
    "project_picker": True,
    "prompt_lab": True,
    "public_read_only": True,
}
